# anchored to the repo root on purpose: an unanchored pattern matches at every
# depth, which once swallowed src/sources/ and produced a commit that did not
# build. see docs/POSTMORTEM.md
/build/
/out/
/target/
/.cache/

# Upstream tarballs, verified against the sha256 in each recipe.
/sources/
